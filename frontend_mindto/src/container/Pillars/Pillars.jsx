import React from 'react';

import { AppWrap } from '../../wrapper';

import './Pillars.scss';

const Pillars = () => {
  return (
    <div>Pillars</div>
  );
};

export default AppWrap(Pillars,'pillar');